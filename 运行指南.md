# Tlias 员工管理系统 - 完整运行指南

## 📋 环境要求

### 必需软件
- **JDK**: 11 或以上版本
- **Maven**: 3.6+ 
- **MySQL**: 5.7 或 8.0+
- **IDE**: IntelliJ IDEA 或 Eclipse（推荐 IDEA）

### 检查环境
```bash
# 检查 Java 版本
java -version

# 检查 Maven 版本
mvn -version

# 检查 MySQL 是否运行
mysql --version
```

## 🗄️ 数据库配置

### 方式一：使用本地数据库（推荐）

#### 1. 启动 MySQL 服务
```bash
# Windows
net start mysql

# Linux/Mac
sudo systemctl start mysql
# 或
sudo service mysql start
```

#### 2. 创建数据库并导入数据
```bash
# 登录 MySQL
mysql -u root -p

# 执行 SQL 文件
source /path/to/database/tlias.sql

# 或者直接在命令行执行
mysql -u root -p < database/tlias.sql
```

#### 3. 修改 application.properties
打开 `src/main/resources/application.properties`，修改为本地数据库配置：

```properties
# 数据库连接配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/tlias?useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=你的MySQL密码

# MyBatis 配置
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis.configuration.map-underscore-to-camel-case=true
```

### 方式二：使用远程数据库（当前配置）

当前项目已配置远程数据库：
- 地址: `82.157.232.106:3306`
- 数据库: `tlias`
- 用户名: `root`
- 密码: `mcy123456`

**注意**: 需要确保网络可以访问该远程服务器，且数据库已经创建好表和数据。

## 🚀 启动项目

### 方式一：使用 Maven 命令行

```bash
# 1. 清理并编译项目
mvn clean compile

# 2. 运行项目
mvn spring-boot:run
```

### 方式二：使用 IDE (IntelliJ IDEA)

1. 打开项目根目录的 `pom.xml`
2. 等待 Maven 依赖下载完成
3. 找到主类 `TliasWebManagementApplication.java`
4. 右键点击 → Run 'TliasWebManagementApplication'

### 方式三：打包运行

```bash
# 1. 打包项目
mvn clean package

# 2. 运行 jar 包
java -jar target/tlias-web-management-0.0.1-SNAPSHOT.jar
```

## ✅ 验证启动成功

### 1. 查看控制台日志
启动成功会看到类似信息：
```
Started TliasWebManagementApplication in 3.456 seconds
Tomcat started on port(s): 8080 (http)
```

### 2. 访问前端页面
浏览器打开: `http://localhost:8080/index.html`

### 3. 测试登录
- 用户名: `admin`
- 密码: `123456`

## 🧪 测试接口

### 使用 HTTP 测试文件

1. 打开 `src/test/api-test.http`
2. 先运行登录接口获取 token
3. 将 token 替换到其他接口的 `YOUR_TOKEN_HERE`
4. 点击运行按钮测试各个接口

### 使用 Postman

导入以下接口进行测试：

#### 登录接口
```
POST http://localhost:8080/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}
```

#### 查询部门列表
```
GET http://localhost:8080/depts
token: 你的token
```

#### 查询员工列表
```
GET http://localhost:8080/emps?page=1&pageSize=10
token: 你的token
```

## ⚠️ 常见问题

### 1. 端口被占用
```
Error: Port 8080 is already in use
```

**解决方案**:
- 修改 `application.properties` 添加: `server.port=8081`
- 或者关闭占用 8080 端口的程序

### 2. 数据库连接失败
```
Communications link failure
```

**解决方案**:
- 检查 MySQL 服务是否启动
- 检查数据库地址、用户名、密码是否正确
- 检查防火墙是否阻止连接
- 如果使用远程数据库，检查网络连接

### 3. Maven 依赖下载失败

**解决方案**:
```bash
# 清理并重新下载
mvn clean install -U

# 或配置国内镜像（阿里云）
# 编辑 ~/.m2/settings.xml
```

### 4. Lombok 注解不生效

**解决方案**:
- IntelliJ IDEA: 安装 Lombok 插件
- Eclipse: 下载 lombok.jar 并安装

### 5. 登录返回 401 或 token 无效

**解决方案**:
- 检查数据库中是否有对应用户
- 确认用户名密码是否正确
- 查看拦截器配置是否正确

### 6. 跨域问题

如果前端单独部署，需要添加 CORS 配置：

```java
// 在 WebConfig.java 中添加
@Override
public void addCorsMappings(CorsRegistry registry) {
    registry.addMapping("/**")
            .allowedOrigins("*")
            .allowedMethods("GET", "POST", "PUT", "DELETE")
            .allowedHeaders("*")
            .allowCredentials(false);
}
```

## 📊 测试数据说明

### 测试账号
- 用户名: `admin` / 密码: `123456`
- 用户名: `zhangwuji` / 密码: `123456`
- 用户名: `zhouzhiruo` / 密码: `123456`

### 部门数据
- 学工部 (ID: 1)
- 教研部 (ID: 2)
- 咨询部 (ID: 3)
- 就业部 (ID: 4)
- 人事部 (ID: 5)

### 员工数据
共 18 条员工记录，包含不同部门和职位

## 🔧 开发建议

### 1. 修改阿里云 OSS 配置
将 `AliOSSUtils.java` 中的硬编码改为配置文件：

```properties
# application.properties
aliyun.oss.endpoint=https://oss-cn-beijing.aliyuncs.com
aliyun.oss.accessKeyId=你的AccessKeyId
aliyun.oss.accessKeySecret=你的AccessKeySecret
aliyun.oss.bucketName=你的BucketName
```

### 2. 密码加密
建议使用 BCrypt 对密码进行加密存储

### 3. 日志配置
可以配置 logback 或 log4j2 进行日志管理

## 📚 项目结构

```
tlias-web-management/
├── src/main/java/com/itheima/
│   ├── controller/          # 控制器层
│   ├── service/             # 业务逻辑层
│   ├── mapper/              # 数据访问层
│   ├── pojo/                # 实体类
│   ├── config/              # 配置类
│   ├── interceptor/         # 拦截器
│   └── utils/               # 工具类
├── src/main/resources/
│   ├── static/              # 静态资源（前端页面）
│   ├── application.properties  # 配置文件
│   └── com/itheima/mapper/  # MyBatis XML
├── src/test/                # 测试文件
├── database/                # 数据库脚本
└── pom.xml                  # Maven 配置
```

## 🎯 下一步

1. ✅ 启动项目
2. ✅ 测试登录功能
3. ✅ 测试部门管理
4. ✅ 测试员工管理
5. 🔄 根据需要扩展功能

## 📞 获取帮助

如果遇到问题：
1. 查看控制台错误日志
2. 检查数据库连接
3. 确认配置文件是否正确
4. 查看本文档的常见问题部分

祝你运行顺利！🎉
